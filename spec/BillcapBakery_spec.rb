require "spec_helper"

describe BillcapBakery do
  it "has a version number" do
    expect(BillcapBakery::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
